---
name: blue_night_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - nodejs # Available for local JS tooling; pre-commit manages its own hook environments.
  - pip:
      - pre-commit>=4.6
      - commitizen>=4.0.0
